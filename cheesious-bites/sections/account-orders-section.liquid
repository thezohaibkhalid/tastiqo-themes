{% comment %}
  Account Orders Section — Order history (Cheesious Bites)
{% endcomment %}

<section class="cb-section cb-account-orders-section">
  <div class="cb-container">
    <div id="orders-page" style="display:none;">
      <div class="cb-account-header">
        <div>
          <h1 class="cb-heading-xl">Order History</h1>
          <p style="color:var(--cb-text-muted);">Your past orders and their status.</p>
        </div>
        <a href="/account" class="cb-btn cb-btn-outline cb-btn-sm">Back to Account</a>
      </div>

      <div id="orders-list" class="cb-orders-list">
        <div style="text-align:center;padding:3rem;color:var(--cb-text-muted);">Loading orders...</div>
      </div>

      <div id="orders-empty" style="display:none;text-align:center;padding:4rem 0;">
        <h2 class="cb-heading-lg">No orders yet</h2>
        <p style="color:var(--cb-text-muted);margin-bottom:1.5rem;">Once you place an order, it will appear here.</p>
        <a href="/menu" class="cb-btn cb-btn-primary">Browse Menu</a>
      </div>

      <div id="orders-pagination" class="cb-orders-pagination" style="display:none;">
        <button class="cb-btn cb-btn-outline cb-btn-sm" id="orders-load-more">Load More</button>
      </div>
    </div>

    <div id="orders-not-logged-in" style="display:none;text-align:center;padding:4rem 0;">
      <h2 class="cb-heading-lg">Sign in to view orders</h2>
      <p style="color:var(--cb-text-muted);margin-bottom:1.5rem;">You need to be signed in to see your order history.</p>
      <button class="cb-btn cb-btn-primary" data-open-auth>Sign In</button>
    </div>
  </div>
</section>
