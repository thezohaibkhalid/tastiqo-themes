{% comment %}
  Header section — editable in the theme editor.
  Renders the sticky top bar with logo, nav, branch picker, and cart.
  Settings allow toggling search/cart icons and choosing mobile menu style.
{% endcomment %}

<header class="cb-header" data-cb-header>
  <div class="cb-header-inner">

    <a href="/" class="cb-logo" aria-label="{{ restaurant.name }}">
      {% if theme.logo_image != blank %}
        <img src="{{ theme.logo_image }}" alt="{{ restaurant.name }}" class="cb-logo-img">
      {% elsif restaurant.logo %}
        <img src="{{ restaurant.logo }}" alt="{{ restaurant.name }}" class="cb-logo-img">
      {% else %}
        <span class="cb-logo-mark">🧀</span>
        <span class="cb-logo-text">{{ restaurant.name | default: "Cheesious Bites" }}</span>
      {% endif %}
    </a>

    <nav class="cb-nav" aria-label="Primary">
      {% for item in navigation.primary %}
        <a href="{{ item.url }}" class="cb-nav-link {% if item.active %}is-active{% endif %}">{{ item.title }}</a>
      {% endfor %}
    </nav>

    <div class="cb-header-right">

      {% if branch_selector and branch_selector.branches and branch_selector.branches.size > 1 %}
        <button type="button" class="cb-branch-trigger" data-cb-branch-trigger aria-label="Choose branch" aria-expanded="false">
          <span class="cb-branch-icon">📍</span>
          <span class="cb-branch-name">{{ branch.name | default: "Pick branch" }}</span>
          <span class="cb-branch-caret">▾</span>
        </button>
        <div class="cb-branch-menu" data-cb-branch-menu hidden>
          <div class="cb-branch-menu-head">Choose your branch</div>
          {% for b in branch_selector.branches %}
            <form method="post" action="/api/storefront/set-branch" class="cb-branch-row {% if branch.id == b.id %}is-selected{% endif %}">
              <input type="hidden" name="branch_id" value="{{ b.id }}">
              <button type="submit">
                <div class="cb-branch-row-name">{{ b.name }}</div>
                {% if b.address %}<div class="cb-branch-row-addr">{{ b.address }}</div>{% endif %}
                {% if b.is_open %}<span class="cb-pill cb-pill-open">Open now</span>{% else %}<span class="cb-pill cb-pill-closed">Closed</span>{% endif %}
              </button>
            </form>
          {% endfor %}
        </div>
      {% endif %}

      {% if section.settings.show_search %}
        <a href="/search" class="cb-icon-btn" aria-label="Search">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
        </a>
      {% endif %}

      <button type="button" class="cb-icon-btn" data-open-auth aria-label="Account">
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"/><circle cx="12" cy="7" r="4"/></svg>
      </button>

      {% if section.settings.show_cart %}
        <a href="/cart" class="cb-cart-link" aria-label="Cart">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="9" cy="21" r="1"/><circle cx="20" cy="21" r="1"/><path d="M1 1h4l2.7 13.4a2 2 0 0 0 2 1.6h9.7a2 2 0 0 0 2-1.6L23 6H6"/></svg>
        </a>
      {% endif %}

      <button type="button" class="cb-mobile-toggle" data-cb-mobile-toggle aria-label="Open menu" aria-expanded="false">
        <span></span><span></span><span></span>
      </button>
    </div>
  </div>

  <div class="cb-mobile-drawer" data-cb-mobile-drawer hidden>
    <nav class="cb-mobile-nav" aria-label="Mobile">
      {% for item in navigation.primary %}
        <a href="{{ item.url }}" class="cb-mobile-link {% if item.active %}is-active{% endif %}">{{ item.title }}</a>
      {% endfor %}
    </nav>
  </div>
</header>

{% schema %}
{
  "name": "Header",
  "settings": [
    { "type": "checkbox", "id": "show_search", "label": "Show search icon", "default": false },
    { "type": "checkbox", "id": "show_cart", "label": "Show cart icon", "default": true },
    {
      "type": "select",
      "id": "mobile_style",
      "label": "Mobile menu style",
      "default": "drawer",
      "options": [
        { "value": "drawer", "label": "Slide-down drawer" },
        { "value": "fullscreen", "label": "Fullscreen overlay" }
      ]
    }
  ],
  "blocks": [
    {
      "type": "nav_link",
      "name": "Nav Link",
      "settings": [
        { "type": "text", "id": "label", "label": "Label", "default": "Link" },
        { "type": "url", "id": "url", "label": "URL", "default": "/" }
      ]
    }
  ],
  "presets": [{ "name": "Header", "settings": {} }]
}
{% endschema %}
