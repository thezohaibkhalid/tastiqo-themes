{% comment %}
  Customer Auth Modal — Login / OTP verification
  Renders a full-screen modal for email + OTP passwordless login.
  JS handles all API calls to /api/storefront/customer/auth/*
{% endcomment %}

<div class="auth-modal-backdrop" id="auth-modal">
  <div class="auth-modal">
    <button class="auth-modal-close" data-close-auth aria-label="Close">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
    </button>

    <!-- Step 1: Email -->
    <div class="auth-step" id="auth-step-email">
      <div class="auth-header">
        <h2>Welcome</h2>
        <p>Sign in or create an account to place orders and track your history.</p>
      </div>
      <form id="auth-email-form" class="auth-form">
        <div class="form-group">
          <label for="auth-email">Email address</label>
          <input type="email" id="auth-email" name="email" placeholder="you@example.com" required autocomplete="email">
        </div>
        <div class="form-group">
          <label for="auth-name">Your name <span class="text-muted">(optional)</span></label>
          <input type="text" id="auth-name" name="full_name" placeholder="Full name" autocomplete="name">
        </div>
        <div class="auth-error" id="auth-email-error" style="display:none;"></div>
        <button type="submit" class="btn btn-primary btn-full" id="auth-email-btn">
          <span>Continue</span>
          <span class="btn-spinner" style="display:none;">
            <svg width="16" height="16" viewBox="0 0 24 24" class="spin"><circle cx="12" cy="12" r="10" fill="none" stroke="currentColor" stroke-width="3" stroke-dasharray="32" stroke-linecap="round"/></svg>
          </span>
        </button>
      </form>
    </div>

    <!-- Step 2: OTP Verification -->
    <div class="auth-step" id="auth-step-otp" style="display:none;">
      <div class="auth-header">
        <h2>Verify your email</h2>
        <p>We sent a 6-digit code to <strong id="auth-otp-email-display"></strong></p>
      </div>
      <form id="auth-otp-form" class="auth-form">
        <div class="form-group">
          <label for="auth-otp">Verification code</label>
          <input type="text" id="auth-otp" name="otp" placeholder="000000" required maxlength="6" inputmode="numeric" autocomplete="one-time-code" style="text-align:center;font-size:1.5rem;letter-spacing:0.5rem;">
        </div>
        <div class="auth-error" id="auth-otp-error" style="display:none;"></div>
        <button type="submit" class="btn btn-primary btn-full" id="auth-otp-btn">
          <span>Verify & Sign In</span>
          <span class="btn-spinner" style="display:none;">
            <svg width="16" height="16" viewBox="0 0 24 24" class="spin"><circle cx="12" cy="12" r="10" fill="none" stroke="currentColor" stroke-width="3" stroke-dasharray="32" stroke-linecap="round"/></svg>
          </span>
        </button>
        <div class="auth-footer">
          <button type="button" class="btn-link" id="auth-resend-btn">Resend code</button>
          <span class="text-muted"> &middot; </span>
          <button type="button" class="btn-link" id="auth-back-btn">Change email</button>
        </div>
      </form>
    </div>

    <!-- Step 3: Success (brief flash) -->
    <div class="auth-step" id="auth-step-success" style="display:none;">
      <div class="auth-header" style="text-align:center;padding:2rem 0;">
        <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="var(--color-success)" stroke-width="2" style="margin:0 auto 1rem;"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>
        <h2>You're signed in!</h2>
        <p id="auth-welcome-name"></p>
      </div>
    </div>
  </div>
</div>

{% comment %} Phone Verification Modal — Verifies a customer phone via SMS OTP {% endcomment %}
<div class="auth-modal-backdrop" id="phone-verify-modal">
  <div class="auth-modal">
    <button class="auth-modal-close" data-close-phone-verify aria-label="Close">
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
    </button>

    <div class="auth-step" id="phone-verify-step-phone">
      <div class="auth-header">
        <h2>Verify your phone</h2>
        <p>Enter your phone number and we'll text you a 6-digit code.</p>
      </div>
      <form id="phone-verify-phone-form" class="auth-form">
        <div class="form-group">
          <label for="phone-verify-phone">Phone number</label>
          <input type="tel" id="phone-verify-phone" name="phone" placeholder="+92 300 1234567" required autocomplete="tel">
        </div>
        <div class="auth-error" id="phone-verify-phone-error" style="display:none;"></div>
        <button type="submit" class="btn btn-primary btn-full" id="phone-verify-send-btn">
          <span>Send code</span>
          <span class="btn-spinner" style="display:none;">
            <svg width="16" height="16" viewBox="0 0 24 24" class="spin"><circle cx="12" cy="12" r="10" fill="none" stroke="currentColor" stroke-width="3" stroke-dasharray="32" stroke-linecap="round"/></svg>
          </span>
        </button>
      </form>
    </div>

    <div class="auth-step" id="phone-verify-step-otp" style="display:none;">
      <div class="auth-header">
        <h2>Enter the code</h2>
        <p>We sent a 6-digit code to <strong id="phone-verify-phone-display"></strong></p>
      </div>
      <form id="phone-verify-otp-form" class="auth-form">
        <div class="form-group">
          <label for="phone-verify-otp">Verification code</label>
          <input type="text" id="phone-verify-otp" name="otp" placeholder="000000" required maxlength="6" inputmode="numeric" autocomplete="one-time-code" style="text-align:center;font-size:1.5rem;letter-spacing:0.5rem;">
        </div>
        <div class="auth-error" id="phone-verify-otp-error" style="display:none;"></div>
        <button type="submit" class="btn btn-primary btn-full" id="phone-verify-otp-btn">
          <span>Verify phone</span>
          <span class="btn-spinner" style="display:none;">
            <svg width="16" height="16" viewBox="0 0 24 24" class="spin"><circle cx="12" cy="12" r="10" fill="none" stroke="currentColor" stroke-width="3" stroke-dasharray="32" stroke-linecap="round"/></svg>
          </span>
        </button>
        <div class="auth-footer">
          <button type="button" class="btn-link" id="phone-verify-resend-btn">Resend code</button>
          <span class="text-muted"> &middot; </span>
          <button type="button" class="btn-link" id="phone-verify-back-btn">Change number</button>
        </div>
      </form>
    </div>

    <div class="auth-step" id="phone-verify-step-success" style="display:none;">
      <div class="auth-header" style="text-align:center;padding:2rem 0;">
        <svg width="48" height="48" viewBox="0 0 24 24" fill="none" stroke="var(--color-success)" stroke-width="2" style="margin:0 auto 1rem;"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>
        <h2>Phone verified!</h2>
        <p class="text-muted">Your phone number is now confirmed.</p>
      </div>
    </div>
  </div>
</div>
